import "../css/componenets/WebsiteWrapper.css";

// eslint-disable-next-line react/prop-types
export default function WebsiteWrapper({ children }) {
  return <div id="website-wrapper">{children}</div>;
}
